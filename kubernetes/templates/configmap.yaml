apiVersion: v1
data:
  .env: |
    DB_HOST=postgresql.postgre.svc.cluster.local
    DB_PORT=5432
    DB_USER=postgres
    DB_PASS=q6PENVTLBW
    DB_NAME=postgres
kind: ConfigMap
metadata:
  name: {{ include "kubernetes.fullname" . }}-env
  labels:
    {{- include "kubernetes.labels" . | nindent 4 }}